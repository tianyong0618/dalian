# 人力资源服务旗舰店 UX 优化方案

## 总体原则
1. **三层信息架构**：关键指标 → 功能工作台 → 数据列表
2. **统一布局**：功能入口统一 col-4 网格，数据区统一表格样式
3. **去"AI"标签**：将"AI智能撮合"等改为自然的功能命名
4. **统一操作模式**：每行一个主要操作按钮 + 下拉菜单扩展操作

## 修改清单

### 12d-demand-matching.html（最大改动）
- "AI智能撮合" → "撮合工作台"，3个卡片改为3列步骤流（需求采集 → 智能匹配 → 对接跟踪）
- "发起智能撮合"按钮保留在工作台底部
- 合并"本月撮合概览"到顶部统计栏
- "撮合记录"和"最近匹配成功"合并为"撮合动态"
- 保持线下对接活动区不变

### 12a-org-management.html
- "AI运营状态监测" → "运营状态监测"
- 告警横幅简化样式，融入信息层级

### 12c-online-store.html
- 无结构改动，仅微调

### 12b / 12e / 12f
- 微调布局对齐，无结构性改动
